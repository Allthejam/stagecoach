import { RouteAssessment } from '@/types/route';

/**
 * Initial empty routes array.
 * All route risk assessments are created live by the user and persisted directly to the database.
 */
export const initialMockRoutes: RouteAssessment[] = [];
